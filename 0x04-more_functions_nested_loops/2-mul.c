#include "main.h"

/**
 * mul - multiples two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with the first number
 * Return: The result of multiplication between x parame and y parame
 */
int mul(int x, int y)
{
	return (x * y);
}
