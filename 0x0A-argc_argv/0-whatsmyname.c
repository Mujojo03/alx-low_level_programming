#include "main.h"
#include <stdio.h>
/**
 * main - its prints its name
 * @argv: the argument vector
 * @argc: the arguement count
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
